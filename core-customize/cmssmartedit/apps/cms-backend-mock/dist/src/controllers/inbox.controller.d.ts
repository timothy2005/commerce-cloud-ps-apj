export declare class InboxController {
    getWorkflowTasks(): {
        pagination: {
            count: number;
            page: number;
            totalCount: number;
            totalPages: number;
        };
        tasks: never[];
    };
}
