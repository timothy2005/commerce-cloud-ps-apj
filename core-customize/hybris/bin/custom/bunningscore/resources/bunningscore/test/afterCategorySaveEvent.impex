# -----------------------------------------------------------------------
#  Copyright (c) 2020 Bunnings or an affiliate company. All rights reserved.
# -----------------------------------------------------------------------
# Test batch configuration
#
$catalog-id=testCatalog
$catalog-version=Online

$testCategory0-id=testCategory0
$testCategory1-id=testCategory1

$catalogversion=catalogversion(catalog(id),version)[unique=true,default=$catalog-id:$catalog-version]
$supercategories=supercategories(code,catalogversion(catalog(id[default=$catalog-id]),version[default=$catalog-version]))
$prices=europe1prices[translator=de.hybris.platform.europe1.jalo.impex.Europe1PricesTranslator]

INSERT_UPDATE Catalog;id[unique=true];name[lang=de];name[lang=en];defaultCatalog
;$catalog-id;$catalog-id;$catalog-id;true

INSERT_UPDATE CatalogVersion; catalog(id)[unique=true]; version[unique=true]; active;defaultCurrency(isocode)
;$catalog-id;$catalog-version;true;EUR;de,en

INSERT_UPDATE Category; order; code[unique = true] ; name                  ; $supercategories; navLevel(code)[default = L1]; categoryBaseStore(uid)[mode = append];$catalogVersion;
                      ; 2    ; curtains-blinds     ; Curtains & Blinds     ;                 ;                             ; BunnningsStoreAU,BunnningsStoreNZ                        ;